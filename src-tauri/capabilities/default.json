{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the editor window is allowed to reach outside itself.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "opener:allow-open-url",
      "comment": "The footer links, handed to the user's browser. Any https host, because the destinations are not the app's to enumerate; http is left out so a downgraded link fails loudly instead of quietly.",
      "allow": [{ "url": "https://*" }]
    }
  ]
}
